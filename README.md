# Installing Kubernetes related programs

Install brew from https://brew.sh

Install kubectl:
`brew install kubectl`

Download Virtualbox from https://www.virtualbox.org/

Install minikube:
`brew cask install minikube`

Start minikube:
`minikube start`